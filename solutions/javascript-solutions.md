# 🟡 JavaScript Solutions
## Детальные решения всех задач JavaScript курса

---

## 📋 Содержание

- [📝 Тема 1: Переменные и типы данных](#-тема-1-переменные-и-типы-данных)
- [🔄 Тема 2: Условия и циклы](#-тема-2-условия-и-циклы)
- [⚡ Тема 3: Функции](#-тема-3-функции)
- [📦 Тема 4: Массивы и объекты](#-тема-4-массивы-и-объекты)
- [🌐 Тема 5: DOM и события](#-тема-5-dom-и-события)

---

# 📝 **ТЕМА 1: ПЕРЕМЕННЫЕ И ТИПЫ ДАННЫХ**

## 💡 Задача 1.1: Информация о пользователе

### 📋 **Условие:**
Создайте программу для хранения и отображения информации о пользователе.

### ✅ **Решение:**

```javascript
// Объявление переменных с информацией о пользователе
const firstName = "Иван";
const lastName = "Петров";
const age = 25;
const email = "ivan@example.com";
const isSubscribed = true;

// Объединение имени и фамилии
const fullName = firstName + " " + lastName;
// Альтернативный способ (ES6):
// const fullName = `${firstName} ${lastName}`;

// Форматированный вывод
console.log("Пользователь:", fullName);
console.log("Возраст:", age, "лет");
console.log("Email:", email);
console.log("Подписка:", isSubscribed ? "активна" : "неактивна");

// Более элегантный способ (одной строкой):
console.log(`
Пользователь: ${fullName}
Возраст: ${age} лет
Email: ${email}
Подписка: ${isSubscribed ? 'активна' : 'неактивна'}
`);
```

### 🎯 **Объяснение решения:**

1. **Типы данных:**
   - `string` для имени и email
   - `number` для возраста
   - `boolean` для статуса подписки

2. **Конкатенация строк:**
   - Классический способ: `+`
   - Современный способ: template literals с `${}`

3. **Условный оператор:**
   - Тернарный оператор для проверки подписки

### 🔧 **Альтернативные решения:**

```javascript
// Решение с объектом (более продвинутое)
const user = {
    firstName: "Иван",
    lastName: "Петров",
    age: 25,
    email: "ivan@example.com",
    isSubscribed: true,
    
    get fullName() {
        return `${this.firstName} ${this.lastName}`;
    },
    
    getInfo() {
        return `
Пользователь: ${this.fullName}
Возраст: ${this.age} лет
Email: ${this.email}
Подписка: ${this.isSubscribed ? 'активна' : 'неактивна'}
        `;
    }
};

console.log(user.getInfo());
```

### ⚡ **Лучшие практики:**
- ✅ Используйте `const` для неизменяемых значений
- ✅ Применяйте template literals для читаемости
- ✅ Группируйте связанные данные в объекты
- ❌ Избегайте `var` в современном JavaScript

---

## 💡 Задача 1.2: Калькулятор ИМТ

### 📋 **Условие:**
Создайте калькулятор для вычисления индекса массы тела.

### ✅ **Решение:**

```javascript
// Исходные данные
const weight = 70; // кг
const height = 1.75; // метры

// Вычисление ИМТ
const bmi = weight / (height * height);
// Альтернативно: const bmi = weight / Math.pow(height, 2);

// Округление до 2 знаков после запятой
const bmiRounded = Math.round(bmi * 100) / 100;
// Альтернативно: const bmiRounded = Number(bmi.toFixed(2));

// Определение категории
let category;
if (bmiRounded < 18.5) {
    category = "Недостаточный вес";
} else if (bmiRounded < 25) {
    category = "Нормальный вес";
} else if (bmiRounded < 30) {
    category = "Избыточный вес";
} else {
    category = "Ожирение";
}

// Вывод результата
console.log(`Вес: ${weight} кг`);
console.log(`Рост: ${height} м`);
console.log(`ИМТ: ${bmiRounded}`);
console.log(`Категория: ${category}`);
```

### 🎯 **Объяснение решения:**

1. **Математические операции:**
   - Возведение в квадрат: `height * height`
   - Альтернатива: `Math.pow(height, 2)`

2. **Округление:**
   - `Math.round(bmi * 100) / 100` - точное округление
   - `toFixed(2)` - строковое представление с 2 знаками

3. **Условная логика:**
   - Цепочка `if-else if` для определения категории

### 🔧 **Продвинутое решение:**

```javascript
// Функциональный подход
function calculateBMI(weight, height) {
    const bmi = weight / (height ** 2); // ES6 экспонента
    const rounded = Math.round(bmi * 100) / 100;
    
    const categories = [
        { max: 18.5, name: "Недостаточный вес", color: "blue" },
        { max: 25, name: "Нормальный вес", color: "green" },
        { max: 30, name: "Избыточный вес", color: "orange" },
        { max: Infinity, name: "Ожирение", color: "red" }
    ];
    
    const category = categories.find(cat => rounded < cat.max);
    
    return {
        value: rounded,
        category: category.name,
        color: category.color,
        isHealthy: category.color === "green"
    };
}

// Использование
const result = calculateBMI(70, 1.75);
console.log(result);
```

### ⚡ **Лучшие практики:**
- ✅ Выносите логику в функции
- ✅ Используйте массивы объектов для конфигурации
- ✅ Возвращайте объекты с дополнительной информацией
- ✅ Применяйте современный синтаксис (** для степени)

---

## 💡 Задача 1.3: Обмен значений переменных

### 📋 **Условие:**
Реализуйте 3 разных способа обмена значениями двух переменных.

### ✅ **Решение:**

```javascript
// Исходные значения
let a = 10;
let b = 20;

console.log("Исходные значения:");
console.log("a =", a, "b =", b);

// СПОСОБ 1: Временная переменная (классический)
console.log("\n--- СПОСОБ 1: Временная переменная ---");
let temp = a;
a = b;
b = temp;
console.log("После обмена: a =", a, "b =", b);

// Восстанавливаем значения для следующего способа
a = 10;
b = 20;

// СПОСОБ 2: Арифметические операции
console.log("\n--- СПОСОБ 2: Арифметические операции ---");
a = a + b; // a = 30
b = a - b; // b = 30 - 20 = 10
a = a - b; // a = 30 - 10 = 20
console.log("После обмена: a =", a, "b =", b);

// Восстанавливаем значения
a = 10;
b = 20;

// СПОСОБ 3: Деструктуризация ES6 (современный)
console.log("\n--- СПОСОБ 3: Деструктуризация ES6 ---");
[a, b] = [b, a];
console.log("После обмена: a =", a, "b =", b);
```

### 🎯 **Сравнение способов:**

| Способ | Преимущества | Недостатки | Применение |
|--------|-------------|------------|------------|
| **Временная переменная** | ✅ Универсальный<br>✅ Читаемый<br>✅ Работает с любыми типами | ❌ Дополнительная память | Везде, особенно для объектов |
| **Арифметические операции** | ✅ Без дополнительной памяти | ❌ Только для чисел<br>❌ Риск переполнения | Редко, только числа |
| **Деструктуризация ES6** | ✅ Краткий синтаксис<br>✅ Современный<br>✅ Читаемый | ❌ Новый синтаксис (ES6+) | Современная разработка |

### 🔧 **Дополнительные примеры:**

```javascript
// Обмен нескольких переменных
let x = 1, y = 2, z = 3;
[x, y, z] = [z, x, y]; // Циклический сдвиг
console.log(x, y, z); // 3, 1, 2

// Обмен элементов массива
const arr = [1, 2, 3, 4, 5];
[arr[0], arr[4]] = [arr[4], arr[0]];
console.log(arr); // [5, 2, 3, 4, 1]

// Обмен свойств объекта
const obj = { first: "A", second: "B" };
[obj.first, obj.second] = [obj.second, obj.first];
console.log(obj); // { first: "B", second: "A" }
```

### ⚡ **Лучшие практики:**
- ✅ Используйте деструктуризацию в современном коде
- ✅ Временную переменную для сложных типов данных
- ❌ Избегайте арифметического способа для production
- ✅ Комментируйте нестандартные приемы

---

## 💡 Задача 1.4: Анализатор типов данных

### 📋 **Условие:**
Создайте программу для детального анализа типов данных в JavaScript.

### ✅ **Решение:**

```javascript
// Создаем массив с различными типами данных
const testData = [
    // Числа
    42,
    3.14,
    NaN,
    Infinity,
    -Infinity,
    
    // Строки
    "Hello",
    "",
    "123",
    
    // Булевы значения
    true,
    false,
    
    // Специальные значения
    null,
    undefined,
    
    // Объекты
    {},
    { name: "John" },
    [],
    [1, 2, 3],
    
    // Функции
    function() {},
    () => {},
    
    // Другие типы
    new Date(),
    /pattern/,
    Symbol("test")
];

// Функция для получения точного типа
function getExactType(value) {
    if (value === null) return "null";
    if (Array.isArray(value)) return "array";
    if (value instanceof Date) return "date";
    if (value instanceof RegExp) return "regexp";
    return typeof value;
}

// Функция для анализа значения
function analyzeValue(value, index) {
    const typeofResult = typeof value;
    const exactType = getExactType(value);
    
    console.log(`\n--- Элемент ${index + 1} ---`);
    console.log("Значение:", value);
    console.log("typeof:", typeofResult);
    console.log("Точный тип:", exactType);
    
    // Дополнительная информация
    if (exactType === "number") {
        if (isNaN(value)) {
            console.log("Особенность: Это NaN (Not a Number)");
        } else if (!isFinite(value)) {
            console.log("Особенность: Бесконечность");
        } else if (Number.isInteger(value)) {
            console.log("Особенность: Целое число");
        } else {
            console.log("Особенность: Дробное число");
        }
    }
    
    if (exactType === "string") {
        console.log("Длина строки:", value.length);
        if (value.length === 0) {
            console.log("Особенность: Пустая строка");
        }
    }
    
    if (exactType === "null") {
        console.log("Особенность: typeof null === 'object' (известная особенность JS)");
    }
    
    if (exactType === "array") {
        console.log("Длина массива:", value.length);
    }
    
    if (exactType === "object" && value !== null) {
        console.log("Количество свойств:", Object.keys(value).length);
    }
}

// Анализируем все значения
console.log("🔍 АНАЛИЗ ТИПОВ ДАННЫХ В JAVASCRIPT");
console.log("===================================");

testData.forEach(analyzeValue);

// Демонстрация особенностей JavaScript
console.log("\n\n🚨 ОСОБЕННОСТИ И ПОДВОДНЫЕ КАМНИ:");
console.log("================================");

console.log("typeof null:", typeof null); // "object" (баг в JS)
console.log("typeof []:", typeof []); // "object"
console.log("typeof function() {}:", typeof function() {}); // "function"
console.log("Array.isArray([]):", Array.isArray([])); // true
console.log("Array.isArray({}):", Array.isArray({})); // false
console.log("NaN === NaN:", NaN === NaN); // false
console.log("Number.isNaN(NaN):", Number.isNaN(NaN)); // true
```

### 🔧 **Продвинутая версия с классом:**

```javascript
class TypeAnalyzer {
    constructor() {
        this.results = [];
    }
    
    static getDetailedType(value) {
        if (value === null) return "null";
        if (value === undefined) return "undefined";
        if (Array.isArray(value)) return "array";
        if (value instanceof Date) return "date";
        if (value instanceof RegExp) return "regexp";
        if (value instanceof Error) return "error";
        if (typeof value === "object" && value.constructor === Object) return "plain object";
        if (typeof value === "function") {
            return value.constructor.name === "AsyncFunction" ? "async function" : "function";
        }
        return typeof value;
    }
    
    analyze(value) {
        const analysis = {
            value: value,
            basicType: typeof value,
            detailedType: TypeAnalyzer.getDetailedType(value),
            isArray: Array.isArray(value),
            isPrimitive: this.isPrimitive(value),
            isTruthy: !!value,
            isFalsy: !value,
            stringRepresentation: String(value),
            jsonSafe: this.isJSONSafe(value)
        };
        
        this.results.push(analysis);
        return analysis;
    }
    
    isPrimitive(value) {
        return value !== Object(value);
    }
    
    isJSONSafe(value) {
        try {
            JSON.stringify(value);
            return true;
        } catch {
            return false;
        }
    }
    
    getReport() {
        return this.results.map((result, index) => ({
            index: index + 1,
            ...result
        }));
    }
}

// Использование класса
const analyzer = new TypeAnalyzer();
testData.forEach(value => analyzer.analyze(value));
console.table(analyzer.getReport());
```

### ⚡ **Лучшие практики:**
- ✅ Используйте `Array.isArray()` для проверки массивов
- ✅ Помните о `typeof null === "object"`
- ✅ Применяйте `Number.isNaN()` вместо `isNaN()`
- ✅ Создавайте вспомогательные функции для точного определения типов

---

## 📊 **Итоги темы "Переменные и типы данных"**

### 🎯 **Изученные концепции:**
- ✅ Объявление переменных (`const`, `let`)
- ✅ Основные типы данных (string, number, boolean, null, undefined)
- ✅ Конкатенация строк и template literals
- ✅ Математические операции и округление
- ✅ Условные операторы (if-else, тернарный)
- ✅ Деструктуризация ES6
- ✅ Проверка типов данных (`typeof`, `Array.isArray()`)

### 🔧 **Применяемые техники:**
- Функциональное программирование
- Работа с объектами и массивами
- Обработка особых случаев (NaN, null, undefined)
- Modern JavaScript (ES6+)

### 🚀 **Следующий шаг:**
Переходите к изучению [Темы 2: Условия и циклы](#-тема-2-условия-и-циклы)