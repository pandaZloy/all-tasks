# 🎨 WORDPRESS: ОТ НОВИЧКА ДО JUNIOR РАЗРАБОТЧИКА
## 6-месячный план обучения с практическими задачами

---

## 📅 **СТРУКТУРА КУРСА (6 МЕСЯЦЕВ)**

### **МЕСЯЦ 1: ОСНОВЫ WORDPRESS**
- Установка и настройка WordPress
- Структура файлов и папок
- Панель администратора
- Контент: посты, страницы, медиа
- **Проекты:** Простой блог, лендинг

### **МЕСЯЦ 2: ТЕМЫ WORDPRESS**
- Структура темы
- Template Hierarchy
- The Loop и WP Query
- Custom Post Types
- **Проекты:** Кастомная тема с нуля

### **МЕСЯЦ 3: ПЛАГИНЫ WORDPRESS**
- Создание плагинов
- Hooks (Actions & Filters)
- WordPress API
- **Проекты:** Функциональный плагин

### **МЕСЯЦ 4: ПРОДВИНУТАЯ РАЗРАБОТКА**
- Custom Fields (ACF)
- REST API
- Gutenberg Blocks
- **Проекты:** Динамический сайт с кастомными полями

### **МЕСЯЦ 5: ОПТИМИЗАЦИЯ И БЕЗОПАСНОСТЬ**
- Производительность
- SEO оптимизация
- Безопасность WordPress
- **Проекты:** Оптимизация существующего сайта

### **МЕСЯЦ 6: ПОДГОТОВКА К РАБОТЕ**
- WooCommerce разработка
- Multisite
- Портфолио проектов
- **Проекты:** Интернет-магазин на WooCommerce

---

# 🚀 **МЕСЯЦ 1: ОСНОВЫ WORDPRESS** (20 задач)

## Задача WP-1: Установка WordPress локально
**Уровень сложности:** ⭐  
**Концепции:** Local environment, XAMPP/MAMP, Docker

**Условие:**
Установите WordPress на локальный сервер разными способами.

**Требования:**
- Установка через XAMPP/MAMP
- Установка через Local by Flywheel
- Установка через Docker (Docker Compose)
- Создание базы данных MySQL
- Настройка wp-config.php
- Доступ к сайту через localhost

**Дополнительно:**
- Настройте несколько WordPress установок на одном хосте
- Изучите структуру папок wp-content, wp-includes, wp-admin

---

## Задача WP-2: Панель администратора
**Уровень сложности:** ⭐  
**Концепции:** Dashboard, меню администратора, настройки

**Условие:**
Освойте навигацию и основные функции админ-панели WordPress.

**Требования:**
- Изучите все разделы Dashboard
- Настройте Settings (General, Reading, Writing, Discussion)
- Настройте Permalinks (ЧПУ)
- Управление пользователями и ролями
- Установите и активируйте тему
- Установите и активируйте плагины

**Задание:**
Создайте документ с описанием назначения каждого пункта меню админки

---

## Задача WP-3: Работа с постами
**Уровень сложности:** ⭐  
**Концепции:** Posts, Categories, Tags, Featured Image

**Условие:**
Создайте и настройте структуру постов для блога.

**Требования:**
- Создайте 10 постов с различным контентом
- Добавьте категории (минимум 5)
- Добавьте теги к постам
- Установите Featured Images (миниатюры)
- Используйте Gutenberg блоки (параграфы, заголовки, изображения, галерея)
- Настройте статусы постов (черновик, опубликовано, запланировано)

**Дополнительно:**
- Запланируйте публикацию постов на будущее
- Создайте sticky post (закрепленный пост)

---

## Задача WP-4: Работа со страницами
**Уровень сложности:** ⭐  
**Концепции:** Pages vs Posts, Page Templates, Parent/Child pages

**Условие:**
Создайте структуру статических страниц сайта.

**Требования:**
- Создайте главную страницу (Home)
- Создайте страницу "О нас"
- Создайте страницу "Контакты" с формой
- Создайте страницу "Услуги" с подстраницами
- Настройте иерархию страниц (родитель-дочерние)
- Установите главную страницу в Settings → Reading

**Дополнительно:**
- Создайте шаблон страницы
- Добавьте кастомные атрибуты страниц

---

## Задача WP-5: Меню навигации
**Уровень сложности:** ⭐⭐  
**Концепции:** Navigation Menus, Menu Locations, Custom Links

**Условие:**
Создайте и настройте различные типы меню.

**Требования:**
- Создайте главное меню (Header Menu)
- Создайте меню футера (Footer Menu)
- Добавьте страницы в меню
- Добавьте кастомные ссылки
- Создайте подменю (dropdown)
- Добавьте CSS классы к пунктам меню

**Дополнительно:**
- Создайте мега-меню с виджетами
- Добавьте иконки к пунктам меню

---

## Задача WP-6: Виджеты и сайдбары
**Уровень сложности:** ⭐⭐  
**Концепции:** Widgets, Widget Areas, Sidebars

**Условие:**
Настройте виджетные области сайта.

**Требования:**
- Добавьте виджеты в сайдбар (поиск, категории, последние посты)
- Создайте виджетную область в футере
- Используйте текстовый виджет с HTML
- Добавьте виджет навигационного меню
- Настройте виджет календаря
- Используйте виджет тегов

**Дополнительно:**
- Создайте кастомный виджет (PHP)
- Условное отображение виджетов на разных страницах

---

## Задача WP-7: Медиабиблиотека
**Уровень сложности:** ⭐⭐  
**Концепции:** Media Library, Image sizes, Attachments

**Условие:**
Освойте работу с медиафайлами в WordPress.

**Требования:**
- Загрузите различные типы файлов (изображения, PDF, видео)
- Отредактируйте изображения (обрезка, поворот, масштабирование)
- Создайте галерею изображений
- Настройте размеры миниатюр в Settings → Media
- Добавьте alt-текст и описания к изображениям
- Организуйте файлы по папкам (плагин или вручную)

**Дополнительно:**
- Оптимизируйте изображения (плагины для сжатия)
- Настройте ленивую загрузку изображений

---

## Задача WP-8: Пользователи и роли
**Уровень сложности:** ⭐⭐  
**Концепции:** User Roles, Capabilities, User Management

**Условие:**
Создайте систему пользователей с разными правами.

**Требования:**
- Создайте пользователей с разными ролями:
  - Administrator (полный доступ)
  - Editor (редактирование контента)
  - Author (создание своих постов)
  - Contributor (создание черновиков)
  - Subscriber (только чтение)
- Протестируйте возможности каждой роли
- Настройте профили пользователей

**Дополнительно:**
- Создайте кастомную роль с определенными правами
- Ограничьте доступ к определенным разделам админки

---

## Задача WP-9: Настройка WordPress
**Уровень сложности:** ⭐⭐  
**Концепции:** wp-config.php, константы, отладка

**Условие:**
Изучите и настройте конфигурационный файл WordPress.

**Требования:**
- Настройте подключение к базе данных
- Измените префикс таблиц БД
- Включите режим отладки (WP_DEBUG)
- Настройте константы для безопасности
- Увеличьте лимит памяти (WP_MEMORY_LIMIT)
- Отключите редактор файлов (DISALLOW_FILE_EDIT)

**Код для wp-config.php:**
```php
// Отладка
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
define('WP_DEBUG_DISPLAY', false);

// Память
define('WP_MEMORY_LIMIT', '256M');

// Безопасность
define('DISALLOW_FILE_EDIT', true);
```

---

## Задача WP-10: Установка и настройка темы
**Уровень сложности:** ⭐⭐  
**Концепции:** Themes, Theme Customizer, Child Themes

**Условие:**
Установите и настройте готовую тему WordPress.

**Требования:**
- Установите популярную тему (Astra, GeneratePress, OceanWP)
- Настройте тему через Customizer:
  - Цвета и типографика
  - Логотип и фавикон
  - Лейаут (ширина контента, сайдбары)
  - Header и Footer
- Создайте дочернюю тему (Child Theme)
- Кастомизируйте CSS через дочернюю тему

**Структура дочерней темы:**
```
child-theme/
├── style.css
└── functions.php
```

---

## Задача WP-11: Плагины - основы
**Уровень сложности:** ⭐⭐  
**Концепции:** Plugin installation, Must-use plugins, Plugin conflicts

**Условие:**
Установите и настройте набор обязательных плагинов.

**Обязательные плагины:**
- Yoast SEO или Rank Math (SEO)
- Contact Form 7 или WPForms (формы)
- Wordfence или Sucuri (безопасность)
- WP Super Cache или W3 Total Cache (кэширование)
- Akismet (защита от спама)
- UpdraftPlus (резервное копирование)

**Требования:**
- Установите и активируйте каждый плагин
- Настройте базовые параметры
- Протестируйте работу форм
- Создайте резервную копию сайта

---

## Задача WP-12: SEO оптимизация - базовая
**Уровень сложности:** ⭐⭐  
**Концепции:** Meta tags, XML Sitemap, Robots.txt

**Условие:**
Настройте базовую SEO оптимизацию сайта.

**Требования:**
- Установите Yoast SEO
- Настройте мета-заголовки и описания
- Создайте XML карту сайта
- Настройте robots.txt
- Оптимизируйте заголовки постов (H1, H2)
- Добавьте alt-теги к изображениям
- Настройте canonical URLs

**Дополнительно:**
- Настройте Open Graph для социальных сетей
- Добавьте структурированные данные (Schema.org)

---

## Задача WP-13: Формы обратной связи
**Уровень сложности:** ⭐⭐  
**Концепции:** Contact forms, Form validation, Email notifications

**Условие:**
Создайте различные типы форм для сайта.

**Требования:**
- Создайте простую контактную форму (имя, email, сообщение)
- Создайте форму заявки с дополнительными полями
- Настройте email уведомления
- Добавьте защиту от спама (reCAPTCHA)
- Кастомизируйте стили формы
- Создайте страницу благодарности после отправки

**Используйте:**
- Contact Form 7 или WPForms

---

## Задача WP-14: Импорт и экспорт контента
**Уровень сложности:** ⭐⭐  
**Концепции:** WordPress Importer, XML export, Migration

**Условие:**
Освойте перенос контента между сайтами WordPress.

**Требования:**
- Экспортируйте контент (Tools → Export)
- Импортируйте контент на другой сайт
- Экспортируйте и импортируйте медиафайлы
- Используйте All-in-One WP Migration для полного переноса
- Смените домен сайта (поиск и замена в БД)

**Инструменты:**
- WordPress Importer (встроенный)
- All-in-One WP Migration (плагин)
- Search and Replace DB (скрипт)

---

## Задача WP-15: Мультиязычность
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** WPML, Polylang, Translation

**Условие:**
Создайте мультиязычный сайт.

**Требования:**
- Установите плагин для мультиязычности (Polylang или WPML)
- Добавьте 2-3 языка
- Переведите страницы и посты
- Создайте языковой переключатель
- Переведите меню и виджеты
- Настройте языковые URL (домен/поддомен/папка)

**Дополнительно:**
- Автоматический перевод через API
- Разные темы для разных языков

---

## Задача WP-16: Кастомизация Gutenberg
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** Block Editor, Core blocks, Block patterns

**Условие:**
Освойте расширенные возможности блочного редактора.

**Требования:**
- Используйте все основные блоки Gutenberg
- Создайте переиспользуемые блоки (Reusable blocks)
- Используйте паттерны блоков
- Настройте стили блоков
- Создайте сложный лейаут с колонками и группами
- Используйте блоки для виджетов

**Блоки для изучения:**
- Columns, Group, Cover
- Gallery, Media & Text
- Table, Buttons, Spacer

---

## Задача WP-17: Производительность - базовая
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** Caching, Image optimization, Minification

**Условие:**
Оптимизируйте скорость загрузки сайта.

**Требования:**
- Установите плагин кэширования (WP Super Cache)
- Оптимизируйте изображения (Smush или ShortPixel)
- Минифицируйте CSS и JavaScript (Autoptimize)
- Включите Gzip сжатие
- Используйте CDN (Cloudflare)
- Отложите загрузку JavaScript (defer/async)
- Измерьте скорость (Google PageSpeed Insights)

**Цель:**
PageSpeed Score > 80

---

## Задача WP-18: Резервное копирование
**Уровень сложности:** ⭐⭐  
**Концепции:** Backups, Restore, Automation

**Условие:**
Настройте систему автоматического резервного копирования.

**Требования:**
- Установите UpdraftPlus или BackWPup
- Настройте автоматическое резервное копирование
- Сохраняйте бэкапы в облако (Dropbox, Google Drive)
- Создайте расписание (ежедневно/еженедельно)
- Протестируйте восстановление из бэкапа
- Включите БД и файлы в бэкап

---

## Задача WP-19: Безопасность - базовая
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** Security hardening, Firewall, Malware scanning

**Условие:**
Защитите WordPress сайт от основных угроз.

**Требования:**
- Установите Wordfence Security
- Настройте файрвол
- Включите двухфакторную аутентификацию
- Скройте версию WordPress
- Ограничьте попытки входа
- Сканирование на вирусы и вредоносный код
- Измените стандартный URL входа (/wp-admin)
- Запретите выполнение PHP в uploads

**Код для .htaccess (запрет PHP в uploads):**
```apache
<Directory "/wp-content/uploads/">
    <Files "*.php">
        Order Deny,Allow
        Deny from All
    </Files>
</Directory>
```

---

## Задача WP-20: WordPress Multisite - знакомство
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** Network setup, Subsites, Network Admin

**Условие:**
Создайте WordPress мультисайт сеть.

**Требования:**
- Активируйте Multisite в wp-config.php
- Настройте тип сети (поддомены или папки)
- Создайте 2-3 подсайта
- Управляйте темами и плагинами на уровне сети
- Настройте отдельных администраторов для подсайтов
- Изучите Network Admin панель

---

# 🎨 **МЕСЯЦ 2: ТЕМЫ WORDPRESS** (25 задач)

## Задача THEME-1: Структура темы WordPress
**Уровень сложности:** ⭐⭐  
**Концепции:** Theme files, style.css, functions.php

**Условие:**
Создайте минимальную рабочую тему с нуля.

**Обязательные файлы:**
```
mytheme/
├── style.css
├── index.php
├── functions.php
├── screenshot.png
```

**style.css заголовок:**
```css
/*
Theme Name: My Custom Theme
Theme URI: https://example.com
Author: Your Name
Author URI: https://example.com
Description: Custom WordPress theme
Version: 1.0
License: GNU General Public License v2 or later
License URI: http://www.gnu.org/licenses/gpl-2.0.html
Text Domain: mytheme
*/
```

**Требования:**
- Создайте минимальную тему
- Зарегистрируйте меню и сайдбар
- Добавьте поддержку миниатюр
- Активируйте и протестируйте тему

---

## Задача THEME-2: Template Hierarchy
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** Template files, Hierarchy rules

**Условие:**
Изучите иерархию шаблонов WordPress и создайте специализированные шаблоны.

**Создайте шаблоны:**
- `index.php` - основной шаблон
- `header.php` - шапка сайта
- `footer.php` - подвал сайта
- `sidebar.php` - боковая панель
- `single.php` - отдельный пост
- `page.php` - статическая страница
- `archive.php` - архив постов
- `404.php` - страница ошибки
- `search.php` - результаты поиска

**Требования:**
- Используйте `get_header()`, `get_footer()`, `get_sidebar()`
- Создайте уникальный дизайн для каждого типа страницы
- Изучите порядок выбора шаблонов WordPress

---

## Задача THEME-3: The Loop
**Уровень сложности:** ⭐⭐  
**Концепции:** WordPress Loop, Template tags

**Условие:**
Освойте WordPress Loop для вывода постов.

**Базовый Loop:**
```php
<?php
if (have_posts()) :
    while (have_posts()) : the_post();
        ?>
        <article id="post-<?php the_ID(); ?>" <?php post_class(); ?>>
            <h2><a href="<?php the_permalink(); ?>"><?php the_title(); ?></a></h2>
            <div class="meta">
                <?php the_author(); ?> | <?php the_date(); ?>
            </div>
            <div class="content">
                <?php the_excerpt(); ?>
            </div>
        </article>
        <?php
    endwhile;
else :
    echo '<p>Постов не найдено</p>';
endif;
?>
```

**Требования:**
- Выведите список постов на главной
- Добавьте миниатюры постов
- Выведите категории и теги
- Добавьте пагинацию
- Создайте альтернативный вывод (без постов)

---

## Задача THEME-4: WP_Query - пользовательские запросы
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** WP_Query, Custom queries, Query parameters

**Условие:**
Создайте кастомные запросы для вывода специфичного контента.

**Примеры запросов:**
```php
// Последние 5 постов из категории
$args = array(
    'posts_per_page' => 5,
    'category_name' => 'news'
);
$query = new WP_Query($args);

// Посты с определенным тегом
$args = array(
    'tag' => 'featured',
    'orderby' => 'date',
    'order' => 'DESC'
);

// Посты за последний месяц
$args = array(
    'date_query' => array(
        array(
            'after' => '1 month ago'
        )
    )
);
```

**Требования:**
- Создайте 5 различных пользовательских запросов
- Выведите популярные посты (по количеству комментариев)
- Выведите похожие посты (по тегам)
- Создайте слайдер из избранных постов
- Используйте pre_get_posts для изменения главного запроса

---

## Задача THEME-5: Template Tags
**Уровень сложности:** ⭐⭐  
**Концепции:** WordPress template tags, Conditional tags

**Условие:**
Освойте основные теги шаблонов WordPress.

**Основные теги:**
```php
// Контент
the_title();
the_content();
the_excerpt();
the_permalink();

// Автор
the_author();
the_author_meta('display_name');

// Дата
the_date();
the_time();

// Категории и теги
the_category();
the_tags();

// Миниатюра
the_post_thumbnail();

// Условия
is_home();
is_single();
is_page();
is_category();
```

**Требования:**
- Создайте шаблон с использованием всех основных тегов
- Используйте условные теги для разного вывода
- Создайте breadcrumbs навигацию
- Добавьте информацию об авторе под постом

---

## Задача THEME-6: Регистрация меню и виджетов
**Уровень сложности:** ⭐⭐  
**Концепции:** register_nav_menus, register_sidebar

**Условие:**
Зарегистрируйте области для меню и виджетов в теме.

**functions.php:**
```php
// Регистрация меню
function mytheme_register_menus() {
    register_nav_menus(array(
        'primary' => __('Primary Menu', 'mytheme'),
        'footer' => __('Footer Menu', 'mytheme')
    ));
}
add_action('init', 'mytheme_register_menus');

// Регистрация сайдбара
function mytheme_widgets_init() {
    register_sidebar(array(
        'name' => __('Sidebar', 'mytheme'),
        'id' => 'sidebar-1',
        'before_widget' => '<div class="widget">',
        'after_widget' => '</div>',
        'before_title' => '<h3 class="widget-title">',
        'after_title' => '</h3>'
    ));
}
add_action('widgets_init', 'mytheme_widgets_init');
```

**Требования:**
- Зарегистрируйте 2-3 меню
- Зарегистрируйте несколько виджетных областей
- Выведите меню в header.php
- Выведите виджеты в sidebar.php

---

## Задача THEME-7: Подключение стилей и скриптов
**Уровень сложности:** ⭐⭐  
**Концепции:** wp_enqueue_style, wp_enqueue_script

**Условие:**
Правильно подключите CSS и JavaScript файлы к теме.

**functions.php:**
```php
function mytheme_enqueue_scripts() {
    // Стили
    wp_enqueue_style('mytheme-style', get_stylesheet_uri());
    wp_enqueue_style('custom-style', get_template_directory_uri() . '/css/custom.css');
    
    // Скрипты
    wp_enqueue_script('mytheme-script', get_template_directory_uri() . '/js/main.js', array('jquery'), '1.0', true);
    
    // Локализация для AJAX
    wp_localize_script('mytheme-script', 'mythemeAjax', array(
        'ajaxurl' => admin_url('admin-ajax.php')
    ));
}
add_action('wp_enqueue_scripts', 'mytheme_enqueue_scripts');
```

**Требования:**
- Подключите основной стиль темы
- Подключите дополнительные CSS файлы
- Подключите JavaScript с зависимостями
- Используйте версионность файлов
- Подключите jQuery правильно

---

## Задача THEME-8: Theme Customizer API
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** Customizer, Sections, Settings, Controls

**Условие:**
Создайте настройки темы через Theme Customizer.

**functions.php:**
```php
function mytheme_customize_register($wp_customize) {
    // Секция
    $wp_customize->add_section('mytheme_colors', array(
        'title' => __('Theme Colors', 'mytheme'),
        'priority' => 30
    ));
    
    // Настройка
    $wp_customize->add_setting('primary_color', array(
        'default' => '#0073aa',
        'sanitize_callback' => 'sanitize_hex_color'
    ));
    
    // Контрол
    $wp_customize->add_control(new WP_Customize_Color_Control($wp_customize, 'primary_color', array(
        'label' => __('Primary Color', 'mytheme'),
        'section' => 'mytheme_colors'
    )));
}
add_action('customize_register', 'mytheme_customize_register');
```

**Требования:**
- Создайте секции для цветов, логотипа, типографики
- Добавьте контролы: цвет, текст, изображение, чекбокс
- Выведите настройки в CSS через `wp_head`
- Добавьте живой предпросмотр (live preview)

---

## Задача THEME-9: Custom Post Types
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** register_post_type, Custom taxonomies

**Условие:**
Создайте кастомный тип записей для портфолио.

**functions.php:**
```php
function mytheme_register_portfolio() {
    $args = array(
        'public' => true,
        'label' => 'Portfolio',
        'labels' => array(
            'name' => 'Portfolio',
            'singular_name' => 'Portfolio Item'
        ),
        'supports' => array('title', 'editor', 'thumbnail'),
        'has_archive' => true,
        'rewrite' => array('slug' => 'portfolio'),
        'menu_icon' => 'dashicons-portfolio'
    );
    register_post_type('portfolio', $args);
    
    // Кастомная таксономия
    register_taxonomy('portfolio_category', 'portfolio', array(
        'hierarchical' => true,
        'label' => 'Portfolio Categories'
    ));
}
add_action('init', 'mytheme_register_portfolio');
```

**Требования:**
- Создайте CPT для портфолио
- Создайте кастомную таксономию
- Создайте шаблон `single-portfolio.php`
- Создайте шаблон `archive-portfolio.php`
- Выведите портфолио на отдельной странице

---

## Задача THEME-10: Custom Fields (без плагинов)
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** add_meta_box, update_post_meta, get_post_meta

**Условие:**
Создайте кастомные поля для постов.

**functions.php:**
```php
function mytheme_add_meta_box() {
    add_meta_box(
        'mytheme_meta_box',
        'Additional Info',
        'mytheme_meta_box_callback',
        'post',
        'normal',
        'high'
    );
}
add_action('add_meta_boxes', 'mytheme_add_meta_box');

function mytheme_meta_box_callback($post) {
    wp_nonce_field('mytheme_save_meta_box', 'mytheme_meta_box_nonce');
    $value = get_post_meta($post->ID, '_mytheme_subtitle', true);
    echo '<label>Subtitle:</label>';
    echo '<input type="text" name="mytheme_subtitle" value="' . esc_attr($value) . '" />';
}

function mytheme_save_meta_box($post_id) {
    if (!isset($_POST['mytheme_meta_box_nonce'])) return;
    if (!wp_verify_nonce($_POST['mytheme_meta_box_nonce'], 'mytheme_save_meta_box')) return;
    if (defined('DOING_AUTOSAVE') && DOING_AUTOSAVE) return;
    
    if (isset($_POST['mytheme_subtitle'])) {
        update_post_meta($post_id, '_mytheme_subtitle', sanitize_text_field($_POST['mytheme_subtitle']));
    }
}
add_action('save_post', 'mytheme_save_meta_box');
```

**Требования:**
- Создайте meta box с несколькими полями
- Сохраните данные с валидацией
- Выведите данные в шаблоне
- Используйте nonce для безопасности

---

## Задача THEME-11: Pagination
**Уровень сложности:** ⭐⭐  
**Концепции:** paginate_links, WP_Query pagination

**Условие:**
Создайте красивую пагинацию для архивов.

**Код пагинации:**
```php
function mytheme_pagination() {
    global $wp_query;
    
    $big = 999999999;
    
    echo paginate_links(array(
        'base' => str_replace($big, '%#%', esc_url(get_pagenum_link($big))),
        'format' => '?paged=%#%',
        'current' => max(1, get_query_var('paged')),
        'total' => $wp_query->max_num_pages,
        'prev_text' => '&laquo; Previous',
        'next_text' => 'Next &raquo;'
    ));
}
```

**Требования:**
- Создайте функцию пагинации
- Добавьте стили для пагинации
- Используйте на archive.php
- Добавьте пагинацию для WP_Query

---

## Задача THEME-12: Breadcrumbs навигация
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** Breadcrumbs, get_ancestors, Schema.org

**Условие:**
Создайте функцию для хлебных крошек.

**Требования:**
- Создайте breadcrumbs для всех типов страниц
- Добавьте Schema.org разметку
- Учтите иерархию страниц
- Добавьте поддержку кастомных типов записей
- Сделайте breadcrumbs SEO-friendly

---

## Задача THEME-13: Ajax Load More
**Уровень сложности:** ⭐⭐⭐⭐  
**Концепции:** AJAX, wp_ajax, infinite scroll

**Условие:**
Реализуйте подгрузку постов через AJAX.

**JavaScript:**
```javascript
jQuery(document).ready(function($) {
    $('#load-more').on('click', function() {
        var button = $(this);
        var page = button.data('page');
        
        $.ajax({
            url: mythemeAjax.ajaxurl,
            type: 'POST',
            data: {
                action: 'load_more_posts',
                page: page
            },
            success: function(response) {
                $('#posts-container').append(response);
                button.data('page', page + 1);
            }
        });
    });
});
```

**PHP (functions.php):**
```php
function load_more_posts() {
    $paged = $_POST['page'];
    
    $args = array(
        'post_type' => 'post',
        'posts_per_page' => 5,
        'paged' => $paged
    );
    
    $query = new WP_Query($args);
    
    if ($query->have_posts()) {
        while ($query->have_posts()) {
            $query->the_post();
            get_template_part('template-parts/content', 'post');
        }
    }
    
    wp_die();
}
add_action('wp_ajax_load_more_posts', 'load_more_posts');
add_action('wp_ajax_nopriv_load_more_posts', 'load_more_posts');
```

**Требования:**
- Реализуйте кнопку "Load More"
- Добавьте infinite scroll
- Покажите loading индикатор
- Обработайте случай когда постов больше нет

---

## Задача THEME-14: Search форма и результаты
**Уровень сложности:** ⭐⭐  
**Концепции:** get_search_form, search.php, search query

**Условие:**
Создайте кастомную форму поиска и страницу результатов.

**searchform.php:**
```php
<form role="search" method="get" class="search-form" action="<?php echo home_url('/'); ?>">
    <label>
        <span class="screen-reader-text">Search for:</span>
        <input type="search" class="search-field" placeholder="Search..." value="<?php echo get_search_query(); ?>" name="s" />
    </label>
    <button type="submit" class="search-submit">Search</button>
</form>
```

**Требования:**
- Создайте кастомную форму поиска
- Создайте шаблон search.php
- Выделите поисковые слова в результатах
- Добавьте фильтры (по типу записи, дате)
- Добавьте "No results" страницу

---

## Задача THEME-15: Comments Template
**Уровень сложности:** ⭐⭐  
**Концепции:** comments_template, comment form, comment list

**Условие:**
Кастомизируйте систему комментариев.

**Требования:**
- Создайте comments.php
- Кастомизируйте форму комментирования
- Стилизуйте список комментариев
- Добавьте вложенные комментарии (threaded)
- Добавьте пагинацию для комментариев
- Покажите количество комментариев

---

## Задача THEME-16: Post Formats
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** add_theme_support, get_post_format

**Условие:**
Добавьте поддержку форматов постов.

**functions.php:**
```php
add_theme_support('post-formats', array(
    'aside',
    'gallery',
    'link',
    'image',
    'quote',
    'status',
    'video',
    'audio',
    'chat'
));
```

**Требования:**
- Добавьте поддержку форматов
- Создайте разные шаблоны для каждого формата
- Используйте `content-{format}.php`
- Добавьте иконки для форматов
- Стилизуйте каждый формат по-разному

---

## Задача THEME-17: Responsive Design
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** Media queries, Mobile-first, Flexbox/Grid

**Условие:**
Сделайте тему полностью адаптивной.

**Требования:**
- Mobile-first подход
- Breakpoints: 320px, 768px, 1024px, 1280px
- Адаптивное меню (hamburger)
- Адаптивные изображения
- Touch-friendly элементы
- Тестирование на разных устройствах

**CSS Media Queries:**
```css
/* Mobile First */
.container {
    width: 100%;
}

/* Tablet */
@media (min-width: 768px) {
    .container {
        width: 750px;
    }
}

/* Desktop */
@media (min-width: 1024px) {
    .container {
        width: 1000px;
    }
}
```

---

## Задача THEME-18: Локализация темы
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** i18n, __(), _e(), text domain

**Условие:**
Сделайте тему готовой к переводу.

**Использование функций локализации:**
```php
// Вывод переведенного текста
echo __('Hello World', 'mytheme');

// Вывод с экранированием
echo esc_html__('Hello World', 'mytheme');

// Прямой вывод
_e('Hello World', 'mytheme');

// С переменной
printf(__('Hello %s', 'mytheme'), $name);
```

**Требования:**
- Оберните все строки в функции локализации
- Создайте файл mytheme.pot
- Создайте перевод на русский язык (.po, .mo)
- Загрузите текстовый домен в functions.php
- Протестируйте смену языка

---

## Задача THEME-19: Child Theme - продвинутый
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** Child themes, Override templates, Hooks

**Условие:**
Создайте продвинутую дочернюю тему.

**Структура:**
```
child-theme/
├── style.css
├── functions.php
├── screenshot.png
├── single.php (override)
├── template-parts/
│   └── content-custom.php
└── inc/
    └── custom-functions.php
```

**functions.php:**
```php
// Подключение стилей родителя
function mytheme_child_enqueue_styles() {
    wp_enqueue_style('parent-style', get_template_directory_uri() . '/style.css');
    wp_enqueue_style('child-style', get_stylesheet_uri(), array('parent-style'));
}
add_action('wp_enqueue_scripts', 'mytheme_child_enqueue_styles');

// Подключение дополнительных функций
require get_stylesheet_directory() . '/inc/custom-functions.php';
```

**Требования:**
- Переопределите несколько шаблонов
- Добавьте кастомные функции
- Используйте хуки родительской темы
- Добавьте новые виджетные области
- Не изменяйте код родительской темы

---

## Задача THEME-20: Page Templates
**Уровень сложности:** ⭐⭐  
**Концепции:** Template Name, Custom page templates

**Условие:**
Создайте кастомные шаблоны страниц.

**template-full-width.php:**
```php
<?php
/*
Template Name: Full Width
*/

get_header(); ?>

<div class="full-width-content">
    <?php
    while (have_posts()) :
        the_post();
        the_content();
    endwhile;
    ?>
</div>

<?php get_footer(); ?>
```

**Требования:**
- Создайте шаблон "Full Width" (без сайдбара)
- Создайте шаблон "Landing Page"
- Создайте шаблон "Portfolio Grid"
- Создайте шаблон "Contact Page"
- Добавьте возможность выбора в админке

---

## Задача THEME-21: Theme Options Page
**Уровень сложности:** ⭐⭐⭐⭐  
**Концепции:** Settings API, Admin pages

**Условие:**
Создайте страницу настроек темы в админке.

**Требования:**
- Создайте страницу в меню админки
- Используйте Settings API
- Добавьте вкладки для разных секций
- Сохраняйте настройки в options
- Валидируйте и sanitize данные
- Добавьте кнопку "Reset to defaults"

---

## Задача THEME-22: Gutenberg Blocks для темы
**Уровень сложности:** ⭐⭐⭐⭐  
**Концепции:** Block Editor, Custom blocks, block.json

**Условие:**
Создайте кастомный блок Gutenberg для темы.

**Требования:**
- Создайте блок "Testimonial"
- Используйте block.json для регистрации
- Добавьте контролы в sidebar
- Стилизуйте блок (editor + frontend)
- Добавьте variations блока

---

## Задача THEME-23: Theme Unit Test
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** Theme testing, WordPress.org requirements

**Условие:**
Протестируйте тему по стандартам WordPress.org.

**Требования:**
- Импортируйте Theme Unit Test Data
- Проверьте все шаблоны
- Валидация HTML/CSS
- Проверка accessibility (a11y)
- Theme Check плагин
- Исправьте все ошибки и предупреждения

---

## Задача THEME-24: Starter Theme - модификация
**Уровень сложности:** ⭐⭐⭐⭐  
**Концепции:** Underscores, Sage, Starter themes

**Условие:**
Возьмите starter theme и кастомизируйте его.

**Популярные starter themes:**
- Underscores (_s)
- Sage by Roots
- Bones
- HTML5 Blank

**Требования:**
- Скачайте и установите Underscores
- Переименуйте под свой проект
- Добавьте свой дизайн
- Интегрируйте Bootstrap или Tailwind
- Добавьте кастомную функциональность

---

## Задача THEME-25: Theme для WordPress.org
**Уровень сложности:** ⭐⭐⭐⭐⭐  
**Концепции:** Theme review, WordPress.org guidelines

**Условие:**
Подготовьте тему для публикации в WordPress.org.

**Требования:**
- Соответствие Theme Review Guidelines
- Лицензия GPL
- Без вшитых ссылок и рекламы
- Все строки локализованы
- Префиксы для всех функций
- Sanitize и escape всех данных
- Прошла Theme Check плагин
- Создайте README.txt
- Подготовьте screenshot.png (1200×900px)

---

# 🔌 **МЕСЯЦ 3: ПЛАГИНЫ WORDPRESS** (25 задач)

## Задача PLUGIN-1: Создание первого плагина
**Уровень сложности:** ⭐  
**Концепции:** Plugin header, Activation, Deactivation

**Условие:**
Создайте простейший плагин WordPress.

**my-first-plugin.php:**
```php
<?php
/*
Plugin Name: My First Plugin
Plugin URI: https://example.com/my-first-plugin
Description: My first WordPress plugin
Version: 1.0
Author: Your Name
Author URI: https://example.com
License: GPL2
Text Domain: my-first-plugin
*/

// Предотвращение прямого доступа
if (!defined('ABSPATH')) {
    exit;
}

// Код плагина
function mfp_hello_world() {
    echo '<p>Hello from my plugin!</p>';
}
add_action('wp_footer', 'mfp_hello_world');

// Активация
function mfp_activate() {
    // Код при активации
    flush_rewrite_rules();
}
register_activation_hook(__FILE__, 'mfp_activate');

// Деактивация
function mfp_deactivate() {
    // Код при деактивации
    flush_rewrite_rules();
}
register_deactivation_hook(__FILE__, 'mfp_deactivate');
```

**Требования:**
- Создайте плагин с корректным заголовком
- Добавьте функциональность
- Обработайте активацию/деактивацию
- Протестируйте установку

---

## Задача PLUGIN-2: Hooks - Actions и Filters
**Уровень сложности:** ⭐⭐  
**Концепции:** add_action, add_filter, do_action, apply_filters

**Условие:**
Изучите систему хуков WordPress.

**Actions:**
```php
// Добавление скриптов
function myplugin_enqueue_scripts() {
    wp_enqueue_script('myplugin-script', plugins_url('js/script.js', __FILE__));
}
add_action('wp_enqueue_scripts', 'myplugin_enqueue_scripts');

// Сохранение поста
function myplugin_save_post($post_id) {
    // Ваш код
}
add_action('save_post', 'myplugin_save_post');
```

**Filters:**
```php
// Изменение контента
function myplugin_modify_content($content) {
    return $content . '<p>Added by plugin</p>';
}
add_filter('the_content', 'myplugin_modify_content');

// Изменение заголовка
function myplugin_modify_title($title) {
    return strtoupper($title);
}
add_filter('the_title', 'myplugin_modify_title');
```

**Требования:**
- Используйте 5 различных actions
- Используйте 5 различных filters
- Создайте собственные хуки
- Изучите приоритеты хуков

---

## Задача PLUGIN-3: Shortcodes
**Уровень сложности:** ⭐⭐  
**Концепции:** add_shortcode, Shortcode API

**Условие:**
Создайте несколько полезных шорткодов.

**Примеры шорткодов:**
```php
// Простой шорткод
function myplugin_button_shortcode($atts) {
    $atts = shortcode_atts(array(
        'text' => 'Click me',
        'url' => '#',
        'color' => 'blue'
    ), $atts);
    
    return '<a href="' . esc_url($atts['url']) . '" class="btn btn-' . esc_attr($atts['color']) . '">' . 
           esc_html($atts['text']) . '</a>';
}
add_shortcode('button', 'myplugin_button_shortcode');

// Использование: [button text="Buy Now" url="/shop" color="green"]

// Шорткод с контентом
function myplugin_box_shortcode($atts, $content = null) {
    return '<div class="custom-box">' . do_shortcode($content) . '</div>';
}
add_shortcode('box', 'myplugin_box_shortcode');

// Использование: [box]Your content here[/box]
```

**Требования:**
- Создайте 5 различных шорткодов
- Используйте атрибуты
- Создайте вложенные шорткоды
- Добавьте кнопку в редактор для вставки

---

## Задача PLUGIN-4: Widgets API
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** WP_Widget, register_widget

**Условие:**
Создайте кастомный виджет.

**Пример виджета:**
```php
class MyPlugin_Recent_Posts_Widget extends WP_Widget {
    
    public function __construct() {
        parent::__construct(
            'myplugin_recent_posts',
            'My Recent Posts',
            array('description' => 'Display recent posts')
        );
    }
    
    public function widget($args, $instance) {
        echo $args['before_widget'];
        
        if (!empty($instance['title'])) {
            echo $args['before_title'] . $instance['title'] . $args['after_title'];
        }
        
        $query = new WP_Query(array(
            'posts_per_page' => $instance['number']
        ));
        
        if ($query->have_posts()) {
            echo '<ul>';
            while ($query->have_posts()) {
                $query->the_post();
                echo '<li><a href="' . get_permalink() . '">' . get_the_title() . '</a></li>';
            }
            echo '</ul>';
        }
        
        echo $args['after_widget'];
    }
    
    public function form($instance) {
        $title = !empty($instance['title']) ? $instance['title'] : 'Recent Posts';
        $number = !empty($instance['number']) ? $instance['number'] : 5;
        ?>
        <p>
            <label for="<?php echo $this->get_field_id('title'); ?>">Title:</label>
            <input class="widefat" id="<?php echo $this->get_field_id('title'); ?>" 
                   name="<?php echo $this->get_field_name('title'); ?>" type="text" 
                   value="<?php echo esc_attr($title); ?>">
        </p>
        <p>
            <label for="<?php echo $this->get_field_id('number'); ?>">Number:</label>
            <input id="<?php echo $this->get_field_id('number'); ?>" 
                   name="<?php echo $this->get_field_name('number'); ?>" type="number" 
                   value="<?php echo esc_attr($number); ?>">
        </p>
        <?php
    }
    
    public function update($new_instance, $old_instance) {
        $instance = array();
        $instance['title'] = sanitize_text_field($new_instance['title']);
        $instance['number'] = absint($new_instance['number']);
        return $instance;
    }
}

function register_myplugin_widget() {
    register_widget('MyPlugin_Recent_Posts_Widget');
}
add_action('widgets_init', 'register_myplugin_widget');
```

**Требования:**
- Создайте 3 кастомных виджета
- Добавьте настройки виджета
- Валидируйте и sanitize данные
- Сделайте виджеты полезными

---

## Задача PLUGIN-5: Admin страница плагина
**Уровень сложности:** ⭐⭐⭐  
**Концепции:** add_menu_page, Settings API

**Условие:**
Создайте страницу настроек плагина в админке.

**Код:**
```php
// Добавление пункта меню
function myplugin_add_admin_menu() {
    add_menu_page(
        'My Plugin Settings',
        'My Plugin',
        'manage_options',
        'myplugin',
        'myplugin_settings_page',
        'dashicons-admin-generic',
        20
    );
}
add_action('admin_menu', 'myplugin_add_admin_menu');

// Страница настроек
function myplugin_settings_page() {
    ?>
    <div class="wrap">
        <h1>My Plugin Settings</h1>
        <form method="post" action="options.php">
            <?php
            settings_fields('myplugin_settings');
            do_settings_sections('myplugin');
            submit_button();
            ?>
        </form>
    </div>
    <?php
}

// Регистрация настроек
function myplugin_settings_init() {
    register_setting('myplugin_settings', 'myplugin_options');
    
    add_settings_section(
        'myplugin_section',
        'Main Settings',
        'myplugin_section_callback',
        'myplugin'
    );
    
    add_settings_field(
        'myplugin_text_field',
        'Text Field',
        'myplugin_text_field_render',
        'myplugin',
        'myplugin_section'
    );
}
add_action('admin_init', 'myplugin_settings_init');

function myplugin_text_field_render() {
    $options = get_option('myplugin_options');
    ?>
    <input type="text" name="myplugin_options[text_field]" 
           value="<?php echo isset($options['text_field']) ? esc_attr($options['text_field']) : ''; ?>">
    <?php
}
```

**Требования:**
- Создайте страницу настроек
- Используйте Settings API
- Добавьте различные типы полей
- Валидация и sanitization
- Добавьте вкладки

---

*Продолжение следует с оставшимися 20 задачами по плагинам, ACF, REST API, WooCommerce и финальными проектами...*

---

# 📚 **РЕКОМЕНДУЕМЫЕ РЕСУРСЫ**

## Официальная документация
- WordPress Codex: https://codex.wordpress.org/
- Developer Resources: https://developer.wordpress.org/
- Theme Handbook: https://developer.wordpress.org/themes/
- Plugin Handbook: https://developer.wordpress.org/plugins/

## Обучающие платформы
- WPBeginner
- WP Tuts+
- WordPress TV

## Инструменты разработчика
- Local by Flywheel
- Query Monitor
- Debug Bar
- Theme Check
- Plugin Check

## Сообщества
- WordPress Slack
- WordPress StackExchange
- r/Wordpress

---

**Готово!** Файл создан, продолжать детализацию остальных задач?