# 🚀 GIT & GITHUB: ПОЛНОЕ РУКОВОДСТВО
## От основ до продвинутых техник

---

## 📚 **СОДЕРЖАНИЕ**

1. [Установка и настройка](#установка-и-настройка)
2. [Основы Git](#основы-git)
3. [Работа с ветками](#работа-с-ветками)
4. [Удалённые репозитории](#удалённые-репозитории)
5. [GitHub workflows](#github-workflows)
6. [Продвинутые команды](#продвинутые-команды)
7. [Работа в команде](#работа-в-команде)
8. [Решение проблем](#решение-проблем)
9. [Best Practices](#best-practices)
10. [Полный список команд](#полный-список-команд)

---

# 📝 **1. УСТАНОВКА И НАСТРОЙКА**

## Установка Git

### macOS:
```bash
# Через Homebrew
brew install git

# Или скачать с официального сайта
# https://git-scm.com/download/mac
```

### Windows:
```bash
# Скачать Git для Windows
# https://git-scm.com/download/win

# Или через Chocolatey
choco install git
```

### Linux (Ubuntu/Debian):
```bash
sudo apt-get update
sudo apt-get install git
```

### Проверка установки:
```bash
git --version
# Вывод: git version 2.40.0
```

---

## Первоначальная настройка

### Установка имени и email (обязательно!):
```bash
# Глобальная настройка для всех репозиториев
git config --global user.name "Ваше Имя"
git config --global user.email "your.email@example.com"

# Локальная настройка для конкретного репозитория
git config user.name "Другое Имя"
git config user.email "other.email@example.com"
```

**Описание:**
- `--global` - настройки применяются ко всем репозиториям
- Без `--global` - только для текущего репозитория
- Имя и email будут видны в истории коммитов

### Настройка редактора по умолчанию:
```bash
# VS Code
git config --global core.editor "code --wait"

# Vim
git config --global core.editor "vim"

# Nano
git config --global core.editor "nano"
```

### Настройка цветного вывода:
```bash
git config --global color.ui auto
```

### Настройка окончаний строк:
```bash
# macOS/Linux
git config --global core.autocrlf input

# Windows
git config --global core.autocrlf true
```

**Описание:**
- `autocrlf` - автоматическое преобразование LF ↔ CRLF
- `input` - конвертировать CRLF в LF при коммите
- `true` - конвертировать LF в CRLF при checkout, обратно при коммите

### Просмотр всех настроек:
```bash
# Все настройки
git config --list

# Конкретная настройка
git config user.name

# Показать откуда взята настройка
git config --show-origin user.name
```

### Создание алиасов (сокращений):
```bash
# Короткие команды
git config --global alias.co checkout
git config --global alias.br branch
git config --global alias.ci commit
git config --global alias.st status

# Полезные алиасы
git config --global alias.unstage 'reset HEAD --'
git config --global alias.last 'log -1 HEAD'
git config --global alias.visual 'log --oneline --graph --all'

# Использование
git co main        # вместо git checkout main
git st             # вместо git status
git visual         # красивая визуализация истории
```

---

# 🎯 **2. ОСНОВЫ GIT**

## Создание репозитория

### Инициализация нового репозитория:
```bash
# Создать папку и инициализировать репозиторий
mkdir my-project
cd my-project
git init
```

**Описание:**
- Создаёт скрытую папку `.git` с метаданными
- Текущая папка становится Git-репозиторием
- Пока нет коммитов и веток (появятся после первого коммита)

### Клонирование существующего репозитория:
```bash
# HTTPS
git clone https://github.com/username/repository.git

# SSH
git clone git@github.com:username/repository.git

# Клонирование в конкретную папку
git clone https://github.com/username/repository.git my-folder

# Клонирование конкретной ветки
git clone -b develop https://github.com/username/repository.git
```

**Описание:**
- Скачивает весь репозиторий со всей историей
- Автоматически создаёт папку с именем репозитория
- Настраивает удалённый репозиторий как `origin`

---

## Базовый workflow

### 1. Проверка статуса:
```bash
git status
```

**Вывод:**
```
On branch main
Your branch is up to date with 'origin/main'.

Changes not staged for commit:
  modified:   index.html

Untracked files:
  style.css
```

**Описание:**
- Показывает текущую ветку
- Файлы с изменениями
- Неотслеживаемые файлы (новые)
- Состояние относительно удалённого репозитория

### 2. Добавление файлов в staging area:
```bash
# Добавить конкретный файл
git add index.html

# Добавить все файлы в текущей папке
git add .

# Добавить все файлы в репозитории
git add -A

# Добавить все файлы определённого типа
git add *.js

# Добавить все изменённые файлы (без новых)
git add -u

# Интерактивное добавление (выбор что добавить)
git add -p
```

**Описание:**
- `git add .` - добавляет файлы из текущей директории и поддиректорий
- `git add -A` - добавляет все изменения во всём репозитории
- `git add -u` - только изменённые и удалённые файлы (не новые)
- `git add -p` - позволяет выбрать части файла для добавления

### 3. Создание коммита:
```bash
# Коммит с сообщением
git commit -m "Добавлена главная страница"

# Коммит с подробным описанием
git commit -m "Заголовок коммита" -m "Детальное описание"

# Добавить все изменения и закоммитить
git commit -am "Обновление стилей"

# Открыть редактор для сообщения
git commit

# Изменить последний коммит
git commit --amend

# Изменить только сообщение последнего коммита
git commit --amend -m "Новое сообщение"
```

**Описание:**
- `-m` - сообщение коммита в одну строку
- `-a` - автоматически добавить все изменённые файлы
- `--amend` - изменить последний коммит (добавить файлы или изменить сообщение)
- Без `-m` откроется редактор для написания сообщения

**Правила хороших коммитов:**
```
Заголовок (до 50 символов)

Детальное описание того, что было сделано и почему.
Можно использовать несколько абзацев.

- Пункт списка
- Ещё один пункт

Fixes #123
```

### 4. Просмотр истории:
```bash
# Полная история
git log

# Сокращённый вывод (одна строка на коммит)
git log --oneline

# С графиком веток
git log --graph --oneline --all

# Последние N коммитов
git log -5

# С изменениями в файлах
git log -p

# Статистика изменений
git log --stat

# История конкретного файла
git log -- index.html

# Коммиты конкретного автора
git log --author="John"

# Коммиты за период
git log --since="2 weeks ago"
git log --after="2023-01-01" --before="2023-12-31"

# Поиск по сообщению
git log --grep="bug fix"

# Красивый формат
git log --pretty=format:"%h - %an, %ar : %s"
```

**Описание форматирования:**
- `%h` - сокращённый hash коммита
- `%H` - полный hash
- `%an` - имя автора
- `%ar` - дата (относительная)
- `%s` - сообщение коммита

### 5. Просмотр изменений:
```bash
# Изменения в рабочей директории (ещё не в staging)
git diff

# Изменения в staging area (готовые к коммиту)
git diff --staged
# или
git diff --cached

# Изменения между коммитами
git diff abc123 def456

# Изменения в конкретном файле
git diff index.html

# Изменения между ветками
git diff main..feature

# Статистика изменений
git diff --stat
```

**Описание:**
- Без параметров - показывает изменения в рабочей директории
- `--staged` - показывает что будет закоммичено
- Использует формат unified diff

---

## Отмена изменений

### Убрать файл из staging area:
```bash
# Убрать конкретный файл
git reset HEAD index.html

# Убрать все файлы
git reset HEAD

# В новых версиях Git
git restore --staged index.html
```

**Описание:**
- Файл остаётся изменённым, но убирается из staging
- `git restore` - новая команда (с Git 2.23)

### Отменить изменения в файле:
```bash
# Отменить изменения в рабочей директории (ОСТОРОЖНО!)
git checkout -- index.html

# В новых версиях Git
git restore index.html

# Отменить все изменения
git restore .
```

**⚠️ ВНИМАНИЕ:** Эта команда безвозвратно удаляет изменения!

### Отменить последний коммит:
```bash
# Отменить коммит, но сохранить изменения в staging
git reset --soft HEAD~1

# Отменить коммит, сохранить изменения в рабочей директории
git reset HEAD~1
# или
git reset --mixed HEAD~1

# Отменить коммит и ВСЕ изменения (ОСТОРОЖНО!)
git reset --hard HEAD~1

# Отменить несколько коммитов
git reset --soft HEAD~3
```

**Описание:**
- `HEAD~1` - один коммит назад
- `HEAD~3` - три коммита назад
- `--soft` - только перемещает HEAD, файлы остаются в staging
- `--mixed` (по умолчанию) - убирает из staging, но файлы остаются изменёнными
- `--hard` - полностью удаляет изменения

### Создать коммит отмены:
```bash
# Создать новый коммит, отменяющий изменения
git revert abc123

# Отменить несколько коммитов
git revert abc123 def456

# Без автоматического коммита
git revert --no-commit abc123
```

**Описание:**
- Создаёт новый коммит, отменяющий изменения
- Безопасный способ отмены (не меняет историю)
- Используется в публичных ветках

---

## .gitignore

### Создание файла .gitignore:
```bash
# Создать файл
touch .gitignore

# Или
nano .gitignore
```

### Примеры шаблонов:
```gitignore
# Игнорировать конкретный файл
config.js

# Игнорировать папку
node_modules/
dist/
build/

# Игнорировать все файлы с расширением
*.log
*.tmp
*.cache

# Игнорировать файлы в любой папке
**/*.log

# НЕ игнорировать конкретный файл (исключение)
!important.log

# Игнорировать только в корне
/TODO

# Игнорировать все .txt файлы в папке doc/
doc/**/*.txt

# Комментарии начинаются с #
```

### Готовые шаблоны .gitignore:
```gitignore
# ===== NODE.JS =====
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local
dist/
build/

# ===== PYTHON =====
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
*.egg-info/

# ===== macOS =====
.DS_Store
.AppleDouble
.LSOverride

# ===== Windows =====
Thumbs.db
ehthumbs.db
Desktop.ini

# ===== IDE =====
.vscode/
.idea/
*.swp
*.swo
*~

# ===== Logs =====
logs/
*.log
npm-debug.log*
```

### Команды для .gitignore:
```bash
# Добавить .gitignore в репозиторий
git add .gitignore
git commit -m "Add .gitignore"

# Удалить уже отслеживаемые файлы из Git (но не с диска)
git rm --cached filename
git rm -r --cached node_modules/

# Проверить, почему файл игнорируется
git check-ignore -v filename

# Показать все игнорируемые файлы
git status --ignored
```

---

# 🌿 **3. РАБОТА С ВЕТКАМИ**

## Основы веток

### Создание и переключение:
```bash
# Показать все ветки
git branch

# Показать все ветки (включая удалённые)
git branch -a

# Создать новую ветку
git branch feature-login

# Переключиться на ветку
git checkout feature-login

# Создать и сразу переключиться (часто используется)
git checkout -b feature-login

# В новых версиях Git
git switch feature-login
git switch -c feature-login
```

**Описание:**
- Ветка - это указатель на коммит
- `checkout -b` - создаёт и переключается одной командой
- `switch` - новая команда для переключения веток (с Git 2.23)

### Переименование веток:
```bash
# Переименовать текущую ветку
git branch -m new-name

# Переименовать другую ветку
git branch -m old-name new-name
```

### Удаление веток:
```bash
# Удалить ветку (только если она слита)
git branch -d feature-login

# Принудительное удаление (даже если не слита)
git branch -D feature-login

# Удалить удалённую ветку
git push origin --delete feature-login
```

---

## Слияние веток (Merge)

### Базовое слияние:
```bash
# Переключиться на целевую ветку
git checkout main

# Слить ветку feature в main
git merge feature-login
```

**Описание:**
- Объединяет изменения из одной ветки в другую
- Создаёт merge commit (если есть изменения в обеих ветках)

### Типы слияния:

#### Fast-forward merge:
```bash
git merge feature
```
**Когда происходит:** Когда в целевой ветке нет новых коммитов
**Результат:** Просто перемещает указатель ветки

#### Three-way merge:
```bash
git merge feature --no-ff
```
**Когда происходит:** Когда есть изменения в обеих ветках
**Результат:** Создаёт новый merge commit

#### Отключить fast-forward:
```bash
# Всегда создавать merge commit
git merge --no-ff feature-login
```

### Отменить слияние:
```bash
# Если ещё не закоммитили
git merge --abort

# Если уже закоммитили
git reset --hard HEAD~1
```

---

## Перебазирование (Rebase)

### Базовое перебазирование:
```bash
# Находясь в feature ветке
git rebase main
```

**Описание:**
- Переносит коммиты на новую базу
- Создаёт линейную историю
- ⚠️ НЕ использовать для публичных веток!

### Интерактивное перебазирование:
```bash
# Изменить последние 3 коммита
git rebase -i HEAD~3
```

**В редакторе появится:**
```
pick abc123 First commit
pick def456 Second commit
pick ghi789 Third commit

# Commands:
# p, pick = использовать коммит
# r, reword = использовать коммит, но изменить сообщение
# e, edit = использовать коммит, но остановиться для правки
# s, squash = использовать коммит, но объединить с предыдущим
# f, fixup = как squash, но отбросить сообщение
# d, drop = удалить коммит
```

**Примеры:**
```bash
# Объединить 3 коммита в один
pick abc123 First commit
squash def456 Second commit
squash ghi789 Third commit

# Изменить порядок коммитов
pick ghi789 Third commit
pick abc123 First commit
pick def456 Second commit

# Удалить коммит
pick abc123 First commit
drop def456 Second commit
pick ghi789 Third commit
```

### Продолжить/отменить rebase:
```bash
# Продолжить после разрешения конфликтов
git rebase --continue

# Пропустить коммит
git rebase --skip

# Отменить rebase
git rebase --abort
```

---

## Разрешение конфликтов

### Когда возникают конфликты:
```bash
git merge feature
# Auto-merging index.html
# CONFLICT (content): Merge conflict in index.html
# Automatic merge failed; fix conflicts and then commit the result.
```

### Просмотр конфликтов:
```bash
# Показать файлы с конфликтами
git status

# Показать конфликты
git diff
```

### Файл с конфликтом выглядит так:
```html
<html>
<head>
<<<<<<< HEAD
    <title>Главная страница</title>
=======
    <title>Home Page</title>
>>>>>>> feature-login
</head>
</html>
```

**Описание:**
- `<<<<<<< HEAD` - ваша версия (текущая ветка)
- `=======` - разделитель
- `>>>>>>> feature-login` - версия из вливаемой ветки

### Разрешение конфликта:
```html
<!-- Выбрать нужный вариант и удалить маркеры -->
<html>
<head>
    <title>Главная страница</title>
</head>
</html>
```

### После разрешения:
```bash
# Добавить разрешённые файлы
git add index.html

# Продолжить слияние
git commit

# Или при rebase
git rebase --continue
```

### Инструменты для разрешения конфликтов:
```bash
# Использовать merge tool
git mergetool

# Принять нашу версию для всех конфликтов
git checkout --ours .

# Принять их версию для всех конфликтов
git checkout --theirs .

# Для конкретного файла
git checkout --ours index.html
git checkout --theirs style.css
```

---

# 🌐 **4. УДАЛЁННЫЕ РЕПОЗИТОРИИ**

## Настройка удалённых репозиториев

### Просмотр удалённых репозиториев:
```bash
# Показать удалённые репозитории
git remote

# Показать с URL
git remote -v

# Вывод:
# origin  https://github.com/user/repo.git (fetch)
# origin  https://github.com/user/repo.git (push)
```

### Добавление удалённого репозитория:
```bash
# Добавить удалённый репозиторий
git remote add origin https://github.com/username/repository.git

# Добавить дополнительный удалённый репозиторий
git remote add upstream https://github.com/original/repository.git
```

### Изменение URL:
```bash
# Изменить URL удалённого репозитория
git remote set-url origin https://github.com/username/new-repo.git

# Изменить с HTTPS на SSH
git remote set-url origin git@github.com:username/repository.git
```

### Удаление удалённого репозитория:
```bash
git remote remove origin
```

### Переименование:
```bash
git remote rename origin upstream
```

---

## Синхронизация с удалённым репозиторием

### Получение изменений (Fetch):
```bash
# Получить изменения из origin (не сливает)
git fetch origin

# Получить из всех удалённых репозиториев
git fetch --all

# Получить и удалить удалённые ветки, которых больше нет
git fetch --prune

# Получить конкретную ветку
git fetch origin main
```

**Описание:**
- Скачивает изменения, но НЕ сливает с вашими
- Безопасная операция
- Позволяет посмотреть изменения перед слиянием

### Получение и слияние (Pull):
```bash
# Получить и слить изменения
git pull origin main

# Pull текущей ветки (если настроен upstream)
git pull

# Pull с rebase вместо merge
git pull --rebase

# Pull с указанием стратегии слияния
git pull --no-ff
```

**Описание:**
- `git pull` = `git fetch` + `git merge`
- `--rebase` создаёт линейную историю

### Отправка изменений (Push):
```bash
# Отправить текущую ветку
git push origin main

# Отправить все ветки
git push --all origin

# Отправить с созданием upstream связи
git push -u origin main
# Теперь можно просто: git push

# Принудительная отправка (ОСТОРОЖНО!)
git push --force origin main
# Безопаснее:
git push --force-with-lease origin main

# Отправить теги
git push --tags
```

**Описание:**
- `-u` или `--set-upstream` - связывает локальную ветку с удалённой
- `--force` - перезаписывает удалённую ветку (опасно!)
- `--force-with-lease` - безопаснее, проверяет что никто не обновлял ветку

---

## Работа с тегами

### Создание тегов:
```bash
# Лёгкий тег (просто указатель)
git tag v1.0.0

# Аннотированный тег (с информацией)
git tag -a v1.0.0 -m "Версия 1.0.0"

# Тег для старого коммита
git tag -a v0.9.0 abc123 -m "Версия 0.9.0"
```

### Просмотр тегов:
```bash
# Все теги
git tag

# Теги с паттерном
git tag -l "v1.*"

# Информация о теге
git show v1.0.0
```

### Отправка тегов:
```bash
# Отправить конкретный тег
git push origin v1.0.0

# Отправить все теги
git push --tags
```

### Удаление тегов:
```bash
# Удалить локально
git tag -d v1.0.0

# Удалить с удалённого репозитория
git push origin --delete v1.0.0
```

### Checkout тега:
```bash
# Переключиться на тег (detached HEAD state)
git checkout v1.0.0

# Создать ветку из тега
git checkout -b version-1 v1.0.0
```

---

# 🐙 **5. GITHUB WORKFLOWS**

## Работа с Pull Request

### 1. Создание Pull Request:
```bash
# Создать ветку для фичи
git checkout -b feature/new-feature

# Внести изменения
# ... редактирование файлов ...

# Закоммитить
git add .
git commit -m "Добавлена новая функция"

# Отправить на GitHub
git push -u origin feature/new-feature

# Создать PR через веб-интерфейс GitHub
# или через GitHub CLI:
gh pr create --title "Новая функция" --body "Описание"
```

### 2. Обновление PR:
```bash
# Внести изменения
git add .
git commit -m "Исправления по ревью"

# Отправить
git push

# Или объединить коммиты перед push
git rebase -i HEAD~3
git push --force-with-lease
```

### 3. Синхронизация с main:
```bash
# Обновить main
git checkout main
git pull origin main

# Вернуться в ветку фичи
git checkout feature/new-feature

# Слить изменения из main
git merge main

# Или сделать rebase
git rebase main
git push --force-with-lease
```

---

## Forking Workflow

### 1. Форк репозитория:
```bash
# На GitHub нажать "Fork"

# Клонировать свой форк
git clone https://github.com/YOUR-USERNAME/repository.git

# Добавить upstream (оригинальный репозиторий)
git remote add upstream https://github.com/ORIGINAL-OWNER/repository.git

# Проверить
git remote -v
```

### 2. Синхронизация с upstream:
```bash
# Получить изменения из upstream
git fetch upstream

# Переключиться на main
git checkout main

# Слить изменения
git merge upstream/main

# Обновить свой форк на GitHub
git push origin main
```

### 3. Создание PR из форка:
```bash
# Создать ветку
git checkout -b feature/awesome-feature

# Внести изменения и закоммитить
git add .
git commit -m "Добавлена крутая функция"

# Отправить в свой форк
git push origin feature/awesome-feature

# Создать PR на GitHub (из вашего форка в оригинальный репозиторий)
```

---

## GitHub CLI (gh)

### Установка:
```bash
# macOS
brew install gh

# Windows
choco install gh

# Linux
sudo apt install gh
```

### Аутентификация:
```bash
gh auth login
```

### Работа с репозиториями:
```bash
# Клонировать репозиторий
gh repo clone username/repository

# Создать новый репозиторий
gh repo create my-project --public

# Просмотр репозитория в браузере
gh repo view --web
```

### Работа с Pull Requests:
```bash
# Создать PR
gh pr create --title "Fix bug" --body "Fixes #123"

# Список PR
gh pr list

# Просмотр PR
gh pr view 42

# Checkout PR
gh pr checkout 42

# Слить PR
gh pr merge 42 --squash

# Закрыть PR
gh pr close 42
```

### Работа с Issues:
```bash
# Создать issue
gh issue create --title "Bug report" --body "Description"

# Список issues
gh issue list

# Просмотр issue
gh issue view 42

# Закрыть issue
gh issue close 42
```

### Работа с Gist:
```bash
# Создать gist
gh gist create file.txt

# Список gists
gh gist list
```

---

## GitHub Actions (основы)

### Пример workflow (.github/workflows/ci.yml):
```yaml
name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    
    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'
    
    - name: Install dependencies
      run: npm install
    
    - name: Run tests
      run: npm test
    
    - name: Build
      run: npm run build
```

### Команды для локальной разработки:
```bash
# Создать workflow файл
mkdir -p .github/workflows
touch .github/workflows/ci.yml

# Добавить и закоммитить
git add .github/workflows/ci.yml
git commit -m "Add CI workflow"
git push

# Просмотр статуса через CLI
gh workflow list
gh run list
gh run view
```

---

# 🔧 **6. ПРОДВИНУТЫЕ КОМАНДЫ**

## Git Stash (Отложенные изменения)

### Сохранение изменений:
```bash
# Сохранить изменения и очистить рабочую директорию
git stash

# С описанием
git stash save "Work in progress on feature"

# Включить неотслеживаемые файлы
git stash -u

# Включить игнорируемые файлы
git stash -a
```

**Описание:**
- Временно сохраняет изменения
- Полезно когда нужно быстро переключиться на другую задачу
- Очищает рабочую директорию

### Просмотр stash:
```bash
# Список всех stash
git stash list

# Показать содержимое последнего stash
git stash show

# Показать детально
git stash show -p

# Показать конкретный stash
git stash show stash@{1}
```

### Применение stash:
```bash
# Применить последний stash (сохранить в списке)
git stash apply

# Применить и удалить из списка
git stash pop

# Применить конкретный stash
git stash apply stash@{2}

# Создать ветку из stash
git stash branch new-branch stash@{1}
```

### Удаление stash:
```bash
# Удалить последний stash
git stash drop

# Удалить конкретный stash
git stash drop stash@{1}

# Удалить все stash
git stash clear
```

---

## Git Cherry-pick

### Применить коммит из другой ветки:
```bash
# Применить один коммит
git cherry-pick abc123

# Применить несколько коммитов
git cherry-pick abc123 def456

# Применить диапазон коммитов
git cherry-pick abc123..def456

# Без автоматического коммита
git cherry-pick --no-commit abc123
```

**Описание:**
- Копирует коммит в текущую ветку
- Создаёт новый коммит с теми же изменениями
- Полезно для переноса исправлений между ветками

### При конфликте:
```bash
# Разрешить конфликты и продолжить
git cherry-pick --continue

# Отменить cherry-pick
git cherry-pick --abort
```

---

## Git Bisect (Поиск бага)

### Бинарный поиск коммита с багом:
```bash
# Начать bisect
git bisect start

# Пометить текущий коммит как плохой
git bisect bad

# Пометить старый коммит как хороший
git bisect good abc123

# Git автоматически переключает на средний коммит
# Тестируем...

# Если баг есть
git bisect bad

# Если бага нет
git bisect good

# Повторяем пока не найдём проблемный коммит

# Завершить bisect
git bisect reset
```

**Описание:**
- Использует бинарный поиск для нахождения коммита с багом
- Автоматически переключается между коммитами
- Эффективно даже на больших историях

### Автоматический bisect:
```bash
# Запустить с автоматическим тестом
git bisect start HEAD abc123
git bisect run npm test

# Git автоматически найдёт проблемный коммит
```

---

## Git Reflog

### Просмотр всей истории действий:
```bash
# Показать историю ссылок
git reflog

# Последние 10 действий
git reflog -10

# Reflog конкретной ветки
git reflog show main
```

**Вывод:**
```
abc123 HEAD@{0}: commit: Add feature
def456 HEAD@{1}: checkout: moving from main to feature
ghi789 HEAD@{2}: commit: Fix bug
```

**Описание:**
- Показывает ВСЕ действия с HEAD
- Полезно для восстановления потерянных коммитов
- Хранится локально (не на удалённом репозитории)

### Восстановление потерянных коммитов:
```bash
# Найти потерянный коммит в reflog
git reflog

# Восстановить
git checkout abc123
# или
git reset --hard abc123
# или создать ветку
git branch recovered abc123
```

---

## Git Blame

### Узнать кто изменил строки:
```bash
# Показать авторов для каждой строки
git blame index.html

# С номерами строк
git blame -L 10,20 index.html

# Игнорировать пробельные изменения
git blame -w index.html

# Показать email вместо имени
git blame -e index.html
```

**Вывод:**
```
abc123 (John Doe 2023-10-01 10:30:45 +0300  1) <html>
def456 (Jane Smith 2023-10-15 14:20:10 +0300  2)   <head>
abc123 (John Doe 2023-10-01 10:30:45 +0300  3)     <title>Page</title>
```

---

## Git Clean

### Удаление неотслеживаемых файлов:
```bash
# Показать что будет удалено (dry-run)
git clean -n

# Удалить неотслеживаемые файлы
git clean -f

# Удалить файлы и папки
git clean -fd

# Удалить вместе с игнорируемыми файлами
git clean -fdx

# Интерактивный режим
git clean -i
```

**⚠️ ОСТОРОЖНО:** Файлы удаляются безвозвратно!

---

## Git Submodules

### Добавление подмодуля:
```bash
# Добавить подмодуль
git submodule add https://github.com/user/library.git libs/library

# Закоммитить
git commit -m "Add library submodule"
```

### Клонирование с подмодулями:
```bash
# Клонировать и инициализировать подмодули
git clone --recursive https://github.com/user/project.git

# Или после обычного клонирования
git submodule init
git submodule update
```

### Обновление подмодулей:
```bash
# Обновить все подмодули
git submodule update --remote

# Обновить конкретный подмодуль
git submodule update --remote libs/library
```

### Удаление подмодуля:
```bash
# Удалить из .gitmodules
git submodule deinit libs/library

# Удалить из .git/modules
rm -rf .git/modules/libs/library

# Удалить из рабочей директории
git rm libs/library

# Закоммитить
git commit -m "Remove library submodule"
```

---

## Git Worktree

### Работа с несколькими рабочими деревьями:
```bash
# Создать новое рабочее дерево
git worktree add ../project-feature feature-branch

# Список рабочих деревьев
git worktree list

# Удалить рабочее дерево
git worktree remove ../project-feature

# Очистить неиспользуемые
git worktree prune
```

**Описание:**
- Позволяет работать с несколькими ветками одновременно
- Каждое worktree - отдельная рабочая директория
- Полезно для параллельной работы над задачами

---

# 👥 **7. РАБОТА В КОМАНДЕ**

## Git Flow

### Основные ветки:
```
main (production)
  └── develop (development)
       ├── feature/login
       ├── feature/dashboard
       └── release/1.0.0
```

### Установка git-flow:
```bash
# macOS
brew install git-flow

# Инициализация
git flow init
```

### Работа с фичами:
```bash
# Начать новую фичу
git flow feature start login
# Создаёт ветку feature/login от develop

# Закончить фичу
git flow feature finish login
# Сливает в develop и удаляет ветку feature/login

# Опубликовать фичу
git flow feature publish login

# Получить фичу
git flow feature pull origin login
```

### Релизы:
```bash
# Начать релиз
git flow release start 1.0.0

# Закончить релиз
git flow release finish 1.0.0
# Сливает в main и develop, создаёт тег
```

### Hotfix:
```bash
# Начать hotfix
git flow hotfix start critical-bug

# Закончить hotfix
git flow hotfix finish critical-bug
# Сливает в main и develop, создаёт тег
```

---

## GitHub Flow (упрощённый)

### Workflow:
```bash
# 1. Создать ветку от main
git checkout -b feature/new-feature main

# 2. Внести изменения и коммитить
git add .
git commit -m "Add new feature"

# 3. Отправить и создать PR
git push -u origin feature/new-feature

# 4. После ревью и тестов - слить в main
# (через веб-интерфейс GitHub)

# 5. Удалить ветку
git branch -d feature/new-feature
git push origin --delete feature/new-feature
```

---

## Code Review Best Practices

### Подготовка к ревью:
```bash
# Убедиться что код чистый
git status

# Проверить что все тесты проходят
npm test

# Сделать rebase на актуальный main
git fetch origin
git rebase origin/main

# Squash мелких коммитов
git rebase -i HEAD~5

# Push
git push --force-with-lease
```

### Получение обратной связи:
```bash
# Получить ветку коллеги для ревью
git fetch origin
git checkout -b review-feature origin/feature-branch

# Или через GitHub CLI
gh pr checkout 123

# Просмотр изменений
git diff main...feature-branch

# Тестирование
npm install
npm test

# Оставить комментарии через GitHub
```

---

## Разрешение конфликтов в команде

### Предотвращение конфликтов:
```bash
# Часто синхронизироваться с main
git fetch origin
git rebase origin/main

# Или
git pull --rebase origin main

# Небольшие атомарные коммиты
git commit -m "Specific change"

# Частые push
git push
```

### Коммуникация:
```bash
# Использовать описательные коммиты
git commit -m "feat: Add user authentication

- Add login form
- Add JWT token handling
- Add protected routes

Closes #123"

# Связывать коммиты с issues
git commit -m "Fix navigation bug

Fixes #456"
```

---

# 🔥 **8. РЕШЕНИЕ ПРОБЛЕМ**

## Отмена изменений

### Отменить последний коммит (НЕ отправленный):
```bash
# Сохранить изменения
git reset --soft HEAD~1

# Убрать из staging, но сохранить файлы
git reset HEAD~1

# Удалить изменения полностью
git reset --hard HEAD~1
```

### Отменить отправленный коммит:
```bash
# Создать коммит отмены
git revert HEAD

# Отменить несколько коммитов
git revert HEAD~3..HEAD

# Отменить конкретный коммит
git revert abc123
```

### Изменить сообщение последнего коммита:
```bash
git commit --amend -m "New message"

# Если уже отправлен
git push --force-with-lease
```

---

## Восстановление потерянных данных

### Восстановить удалённую ветку:
```bash
# Найти в reflog
git reflog

# Восстановить ветку
git branch deleted-branch abc123
```

### Восстановить удалённый коммит:
```bash
# Найти потерянный коммит
git fsck --lost-found

# Или в reflog
git reflog

# Восстановить
git cherry-pick abc123
```

### Восстановить файл из старого коммита:
```bash
# Восстановить файл из определённого коммита
git checkout abc123 -- path/to/file

# Восстановить удалённый файл
git checkout HEAD~1 -- deleted-file.txt
```

---

## Исправление ошибок

### Убрать файл из последнего коммита:
```bash
# Убрать из коммита, но оставить изменения
git reset --soft HEAD~1
git reset HEAD unwanted-file.txt
git commit -c ORIG_HEAD
```

### Изменить старый коммит:
```bash
# Интерактивный rebase
git rebase -i HEAD~5

# Пометить коммит как 'edit'
# Внести изменения
git add .
git commit --amend
git rebase --continue
```

### Разделить коммит:
```bash
# Rebase с edit
git rebase -i HEAD~3

# Отменить коммит
git reset HEAD~1

# Создать несколько коммитов
git add file1.js
git commit -m "Part 1"
git add file2.js
git commit -m "Part 2"

# Продолжить rebase
git rebase --continue
```

---

## Очистка репозитория

### Удалить большой файл из истории:
```bash
# Используя git filter-branch (старый способ)
git filter-branch --tree-filter 'rm -f large-file.zip' HEAD

# Используя git filter-repo (рекомендуется)
# Установка: pip install git-filter-repo
git filter-repo --path large-file.zip --invert-paths

# Force push
git push --force --all
```

### Уменьшить размер репозитория:
```bash
# Очистить недостижимые объекты
git gc --aggressive --prune=now

# Удалить старые reflog записи
git reflog expire --expire=now --all
git gc --prune=now
```

---

## Проблемы с удалённым репозиторием

### Отклонённый push:
```bash
# Ошибка: Updates were rejected
# Решение 1: Pull и merge
git pull origin main
git push

# Решение 2: Pull с rebase
git pull --rebase origin main
git push

# Решение 3: Force push (ОСТОРОЖНО!)
git push --force-with-lease
```

### Изменить URL удалённого репозитория:
```bash
git remote set-url origin new-url
```

### Синхронизация после force push:
```bash
# Если коллега сделал force push
git fetch origin
git reset --hard origin/main
```

---

# ✨ **9. BEST PRACTICES**

## Коммиты

### Правила хороших коммитов:
```bash
# ✅ Хорошие коммиты
git commit -m "Add user authentication"
git commit -m "Fix navigation menu bug"
git commit -m "Update dependencies to latest versions"

# ❌ Плохие коммиты
git commit -m "fixes"
git commit -m "update"
git commit -m "asdfasdf"
```

### Conventional Commits:
```bash
# Формат: <type>(<scope>): <subject>

# Типы
feat:     новая функциональность
fix:      исправление бага
docs:     изменения в документации
style:    форматирование, пропущенные точки с запятой и т.д.
refactor: рефакторинг кода
test:     добавление тестов
chore:    обновление зависимостей, конфигурации и т.д.

# Примеры
git commit -m "feat(auth): add JWT token authentication"
git commit -m "fix(nav): resolve mobile menu overflow issue"
git commit -m "docs(readme): update installation instructions"
git commit -m "refactor(api): simplify error handling logic"
```

### Детальное сообщение:
```bash
git commit -m "feat(auth): add user authentication

- Implement JWT token generation
- Add login and registration endpoints
- Create user model and database schema
- Add password hashing with bcrypt

Closes #123
Breaking Change: Updates API endpoints"
```

---

## Ветки

### Именование веток:
```bash
# Хорошие имена
feature/user-authentication
bugfix/navigation-menu-overflow
hotfix/critical-security-patch
release/v1.2.0

# Плохие имена
john-branch
new-feature
fix
test
```

### Структура:
```
main
├── develop
│   ├── feature/login
│   ├── feature/dashboard
│   └── feature/profile
├── release/1.0.0
└── hotfix/security-patch
```

---

## Работа с историей

### Чистая история:
```bash
# Использовать rebase для линейной истории
git pull --rebase

# Squash мелких коммитов перед PR
git rebase -i HEAD~5

# Использовать --no-ff для важных merge
git merge --no-ff feature-branch
```

### Не изменяйте публичную историю:
```bash
# ❌ Плохо - изменение опубликованной истории
git push --force origin main

# ✅ Хорошо - только для feature веток
git push --force-with-lease origin feature-branch
```

---

## .gitignore стратегия

### Глобальный .gitignore:
```bash
# Создать глобальный .gitignore
touch ~/.gitignore_global

# Настроить Git
git config --global core.excludesfile ~/.gitignore_global
```

### Содержимое ~/.gitignore_global:
```gitignore
# OS
.DS_Store
Thumbs.db

# IDEs
.vscode/
.idea/
*.swp

# Temporary files
*.tmp
*.log
```

---

## Безопасность

### Никогда не коммитьте:
```gitignore
# Секретные ключи
.env
.env.local
config/secrets.yml
*.pem
*.key

# Credentials
credentials.json
secrets.json
```

### Если случайно закоммитили секрет:
```bash
# Удалить из истории
git filter-repo --path .env --invert-paths

# Сменить все секреты!
# Удалить из истории недостаточно - они были опубликованы
```

---

## Производительность

### Для больших репозиториев:
```bash
# Shallow clone (без полной истории)
git clone --depth 1 https://github.com/user/repo.git

# Partial clone (без больших файлов)
git clone --filter=blob:none https://github.com/user/repo.git

# Sparse checkout (только нужные папки)
git sparse-checkout init
git sparse-checkout set frontend/
```

---

# 📖 **10. ПОЛНЫЙ СПИСОК КОМАНД**

## Настройка
```bash
git config --global user.name "Name"          # Установить имя
git config --global user.email "email"        # Установить email
git config --list                             # Показать настройки
git config --global core.editor "code"        # Установить редактор
```

## Создание репозитория
```bash
git init                                      # Инициализировать репозиторий
git clone <url>                               # Клонировать репозиторий
```

## Базовые команды
```bash
git status                                    # Статус изменений
git add <file>                                # Добавить файл
git add .                                     # Добавить все файлы
git commit -m "message"                       # Создать коммит
git commit -am "message"                      # Add + commit
git log                                       # История коммитов
git log --oneline                             # Краткая история
git diff                                      # Изменения
```

## Отмена изменений
```bash
git restore <file>                            # Отменить изменения в файле
git restore --staged <file>                   # Убрать из staging
git reset HEAD~1                              # Отменить последний коммит
git reset --soft HEAD~1                       # Отменить, сохранить в staging
git reset --hard HEAD~1                       # Отменить полностью
git revert <commit>                           # Создать коммит отмены
git commit --amend                            # Изменить последний коммит
```

## Ветки
```bash
git branch                                    # Список веток
git branch <name>                             # Создать ветку
git checkout <name>                           # Переключиться на ветку
git checkout -b <name>                        # Создать и переключиться
git switch <name>                             # Переключиться (новая команда)
git switch -c <name>                          # Создать и переключиться
git branch -d <name>                          # Удалить ветку
git branch -D <name>                          # Принудительно удалить
git merge <branch>                            # Слить ветку
git merge --no-ff <branch>                    # Слить без fast-forward
```

## Удалённые репозитории
```bash
git remote                                    # Список удалённых репозиториев
git remote -v                                 # С URL
git remote add <name> <url>                   # Добавить удалённый
git remote remove <name>                      # Удалить удалённый
git fetch <remote>                            # Получить изменения
git pull                                      # Получить и слить
git pull --rebase                             # Получить с rebase
git push <remote> <branch>                    # Отправить изменения
git push -u <remote> <branch>                 # Отправить и связать ветки
git push --force-with-lease                   # Безопасный force push
```

## Stash
```bash
git stash                                     # Сохранить изменения
git stash save "message"                      # С описанием
git stash list                                # Список stash
git stash apply                               # Применить последний stash
git stash pop                                 # Применить и удалить
git stash drop                                # Удалить stash
git stash clear                               # Удалить все stash
```

## Теги
```bash
git tag                                       # Список тегов
git tag <name>                                # Создать тег
git tag -a <name> -m "message"                # Аннотированный тег
git tag -d <name>                             # Удалить тег
git push --tags                               # Отправить теги
git push origin <tag>                         # Отправить один тег
```

## Продвинутые команды
```bash
git rebase <branch>                           # Перебазировать
git rebase -i HEAD~<n>                        # Интерактивный rebase
git rebase --continue                         # Продолжить rebase
git rebase --abort                            # Отменить rebase
git cherry-pick <commit>                      # Применить коммит
git reflog                                    # История ссылок
git blame <file>                              # Авторы строк
git bisect start                              # Начать bisect
git bisect good/bad                           # Пометить коммит
git clean -n                                  # Показать что будет удалено
git clean -f                                  # Удалить неотслеживаемые файлы
```

## Информация
```bash
git show <commit>                             # Показать коммит
git show <branch>:<file>                      # Показать файл из ветки
git describe                                  # Описание текущего состояния
git shortlog                                  # Статистика коммитов
git log --graph --all                         # График веток
git log --author="name"                       # Коммиты автора
git log --since="2 weeks ago"                 # За период
git log -- <file>                             # История файла
```

## Поиск
```bash
git grep "pattern"                            # Поиск в файлах
git log -S "pattern"                          # Поиск в коммитах
git log --grep="pattern"                      # Поиск по сообщениям
```

## Submodules
```bash
git submodule add <url> <path>                # Добавить подмодуль
git submodule init                            # Инициализировать
git submodule update                          # Обновить подмодули
git submodule update --remote                 # Обновить до последней версии
```

## Worktree
```bash
git worktree add <path> <branch>              # Создать worktree
git worktree list                             # Список worktrees
git worktree remove <path>                    # Удалить worktree
```

## Обслуживание
```bash
git gc                                        # Сборка мусора
git fsck                                      # Проверка целостности
git prune                                     # Удалить недостижимые объекты
```

---

# 🎓 **ШПАРГАЛКА: ЧАСТЫЕ СЦЕНАРИИ**

## Начало работы над новой функцией
```bash
git checkout main
git pull origin main
git checkout -b feature/new-feature
# ... работа ...
git add .
git commit -m "feat: add new feature"
git push -u origin feature/new-feature
```

## Обновление feature ветки
```bash
git checkout main
git pull origin main
git checkout feature/my-feature
git rebase main
git push --force-with-lease
```

## Исправление последнего коммита
```bash
# Изменить файлы
git add .
git commit --amend --no-edit
git push --force-with-lease
```

## Отмена изменений
```bash
# Отменить изменения в файле
git restore filename

# Отменить последний коммит (сохранить изменения)
git reset --soft HEAD~1

# Отменить последний коммит (удалить изменения)
git reset --hard HEAD~1
```

## Синхронизация с форком
```bash
git remote add upstream <original-repo-url>
git fetch upstream
git checkout main
git merge upstream/main
git push origin main
```

## Squash коммитов перед PR
```bash
git rebase -i HEAD~3
# В редакторе заменить 'pick' на 'squash' для нужных коммитов
git push --force-with-lease
```

## Временное сохранение изменений
```bash
git stash save "WIP: feature in progress"
git checkout other-branch
# ... работа ...
git checkout original-branch
git stash pop
```

---

# 📚 **ПОЛЕЗНЫЕ РЕСУРСЫ**

## Документация
- [Git Official Documentation](https://git-scm.com/doc)
- [GitHub Docs](https://docs.github.com)
- [Atlassian Git Tutorials](https://www.atlassian.com/git/tutorials)

## Интерактивное обучение
- [Learn Git Branching](https://learngitbranching.js.org/)
- [Git Immersion](http://gitimmersion.com/)
- [GitHub Skills](https://skills.github.com/)

## Книги
- Pro Git (бесплатно онлайн)
- Git Pocket Guide

## Инструменты
- [GitKraken](https://www.gitkraken.com/) - GUI клиент
- [SourceTree](https://www.sourcetreeapp.com/) - GUI клиент
- [GitHub Desktop](https://desktop.github.com/) - Официальный клиент
- [GitLens](https://marketplace.visualstudio.com/items?itemName=eamodio.gitlens) - VS Code расширение

---

**Готово!** Теперь у вас есть полное руководство по Git и GitHub! 🚀